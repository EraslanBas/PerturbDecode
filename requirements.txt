# Pinned convenience file. The authoritative dependency list lives in
# pyproject.toml; prefer `pip install -e '.[dev]'` for development.
numpy>=1.20
pandas>=1.3
scipy>=1.7
scanpy>=1.9
anndata>=0.8
torch>=1.10
scikit-learn>=1.0
statsmodels>=0.13
pingouin>=0.5
matplotlib>=3.4
seaborn>=0.11
tqdm>=4.60

# Optional R integration (pip install 'PerturbDecode[r]')
# rpy2>=3.4
# anndata2ri>=1.0
